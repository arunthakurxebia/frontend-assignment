import { PtCurrencyPipe } from './pt-currency.pipe';

describe('PtCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new PtCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
